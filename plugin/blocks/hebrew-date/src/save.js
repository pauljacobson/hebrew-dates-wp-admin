/**
 * Save function for the Hebrew Date block.
 *
 * Returns null because this is a dynamic block — all rendering
 * happens server-side via render.php. No HTML is saved in post content;
 * only block attributes are stored in the block delimiter comment.
 *
 * @package Hebrew_Dates_Admin
 * @since   1.1.0
 */
export default function save() {
	return null;
}
